# OCR Stage 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不增加设置项和数据库字段的前提下，为现有 OCR 引入“整图优先、低质量补救”的自动增强策略。

**Architecture:** 保持 [main.rs](/Users/wmy/data/Pycharm_Project/Work_Review/src-tauri/src/main.rs) 的 OCR 调用点不变，把首轮 OCR、质量评估、区域规划、图像预处理和文本合并全部封装进 [ocr.rs](/Users/wmy/data/Pycharm_Project/Work_Review/src-tauri/src/ocr.rs)。策略逻辑优先通过纯单元测试验证，不依赖真实平台 OCR 环境。

**Tech Stack:** Rust、Tauri、`image` crate、现有平台 OCR 引擎

---

### Task 1: 为 OCR 策略新增失败测试

**Files:**
- Modify: `src-tauri/src/ocr.rs`
- Test: `src-tauri/src/ocr.rs`

- [ ] **Step 1: 写低质量判定、区域规划和结果合并的失败测试**

```rust
#[test]
fn 低质量判定应在短文本和空文本时触发补救() {
    assert!(should_retry_after_initial_ocr(None));
    assert!(should_retry_after_initial_ocr(Some(&sample_result("abc"))));
    assert!(!should_retry_after_initial_ocr(Some(&sample_result("项目进展\n完成接口联调"))));
}

#[test]
fn 区域规划应生成不越界的高价值区域() {
    let plans = build_retry_region_plans(1440, 900);
    assert!(!plans.is_empty());
    assert!(plans.iter().all(|plan| plan.x + plan.width <= 1440));
    assert!(plans.iter().all(|plan| plan.y + plan.height <= 900));
}

#[test]
fn 合并结果应去重并补充新增文本() {
    let merged = merge_ocr_results(
        Some(sample_result("项目看板\nIssue 28")),
        vec![sample_result("Issue 28\nPull Request")],
    )
    .unwrap();
    assert_eq!(merged.text, "项目看板\nIssue 28\nPull Request");
}
```

- [ ] **Step 2: 运行测试并确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml ocr::tests -- --nocapture`
Expected: 编译失败或测试失败，提示缺少新增策略函数。

- [ ] **Step 3: 再写策略编排测试**

```rust
#[test]
fn 高质量首轮结果不应触发补救ocr() {
    let mut calls = 0;
    let result = execute_ocr_pipeline_with(
        &image_path,
        |_: &Path| {
            calls += 1;
            Ok(Some(sample_result("项目进展\n完成接口联调\n准备提交测试"))))
        },
    )
    .unwrap()
    .unwrap();

    assert_eq!(calls, 1);
    assert!(result.text.contains("完成接口联调"));
}

#[test]
fn 低质量首轮结果应触发区域补救并合并文本() {
    let mut responses = std::collections::VecDeque::from([
        Some(sample_result("abc")),
        Some(sample_result("顶部标题")),
        Some(sample_result("正文内容")),
    ]);
    let result = execute_ocr_pipeline_with(&image_path, |_| Ok(responses.pop_front().unwrap()))
        .unwrap()
        .unwrap();

    assert!(result.text.contains("顶部标题"));
    assert!(result.text.contains("正文内容"));
}
```

- [ ] **Step 4: 再次运行测试并确认失败原因正确**

Run: `cargo test --manifest-path src-tauri/Cargo.toml ocr::tests -- --nocapture`
Expected: 新增编排测试失败，证明测试覆盖到了尚未实现的策略行为。

### Task 2: 以最小改动实现 OCR 自动增强策略

**Files:**
- Modify: `src-tauri/src/ocr.rs`
- Test: `src-tauri/src/ocr.rs`

- [ ] **Step 1: 拆分单次 OCR 和策略入口**

```rust
pub fn extract_text(&self, image_path: &Path) -> Result<Option<OcrResult>> {
    self.execute_ocr_pipeline(image_path)
}

fn execute_ocr_pipeline(&self, image_path: &Path) -> Result<Option<OcrResult>> {
    execute_ocr_pipeline_with(image_path, |path| self.extract_text_once(path))
}

fn extract_text_once(&self, image_path: &Path) -> Result<Option<OcrResult>> {
    match self.preferred_engine {
        OcrEngine::PaddleOCR => self.extract_with_paddle(image_path),
        #[cfg(target_os = "macos")]
        OcrEngine::MacOSVision => self.extract_with_vision(image_path),
        #[cfg(target_os = "windows")]
        OcrEngine::WindowsOCR => self.extract_with_windows_ocr(image_path),
        #[cfg(target_os = "linux")]
        OcrEngine::Tesseract => self.extract_with_tesseract(image_path),
    }
}
```

- [ ] **Step 2: 实现质量评估、区域规划和预处理**

```rust
fn should_retry_after_initial_ocr(result: Option<&OcrResult>) -> bool { /* 规则判定 */ }

fn build_retry_region_plans(width: u32, height: u32) -> Vec<RetryRegionPlan> { /* 固定比例区域 */ }

fn preprocess_region_for_retry(image: &DynamicImage) -> DynamicImage {
    image
        .grayscale()
        .adjust_contrast(25.0)
        .resize(new_width, new_height, FilterType::Lanczos3)
        .unsharpen(1.0, 1)
}
```

- [ ] **Step 3: 实现临时区域图保存、补救 OCR 和结果合并**

```rust
fn execute_ocr_pipeline_with<F>(image_path: &Path, mut recognize: F) -> Result<Option<OcrResult>>
where
    F: FnMut(&Path) -> Result<Option<OcrResult>>,
{
    let initial = recognize(image_path)?;
    if !should_retry_after_initial_ocr(initial.as_ref()) {
        return Ok(initial);
    }

    let source = image::open(image_path)?;
    let mut retry_results = Vec::new();
    for plan in build_retry_region_plans(source.width(), source.height()) {
        let cropped = crop_region(&source, plan);
        let processed = preprocess_region_for_retry(&cropped);
        let temp_path = save_retry_region_temp_image(&processed, &plan.name)?;
        if let Ok(Some(result)) = recognize(&temp_path) {
            retry_results.push(result);
        }
        let _ = std::fs::remove_file(&temp_path);
    }

    Ok(merge_ocr_results(initial, retry_results))
}
```

- [ ] **Step 4: 运行 OCR 单测并确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml ocr::tests -- --nocapture`
Expected: `ocr::tests` 全部通过。

### Task 3: 回归验证现有 OCR 调用链不受影响

**Files:**
- Modify: `src-tauri/src/ocr.rs`（如需小幅清理）
- Test: `src-tauri/src/main.rs`

- [ ] **Step 1: 运行受影响的现有测试**

Run: `cargo test --manifest-path src-tauri/Cargo.toml screenshot::tests -- --nocapture`
Expected: `screenshot::tests` 全部通过，证明 OCR 输入源相关逻辑未被破坏。

- [ ] **Step 2: 运行主循环相关测试**

Run: `cargo test --manifest-path src-tauri/Cargo.toml main::tests -- --nocapture`
Expected: `main::tests` 全部通过，证明调用点无需额外改动。

- [ ] **Step 3: 格式化并做最终校验**

Run: `cargo fmt --manifest-path src-tauri/Cargo.toml`
Expected: 无报错。

Run: `cargo test --manifest-path src-tauri/Cargo.toml ocr::tests screenshot::tests main::tests -- --nocapture`
Expected: 目标测试全部通过。
